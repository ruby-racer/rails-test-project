<div class="row">
  <div class="col-md-4 col-md-offset-4">
    <%- @breeds.each do |breed| %>
      <p id="<%= urlify(breed) %>">
          <%= link_to  friendly_name(breed), "#{breeds_path}/#{urlify(breed)}" %>
      </p>
    <%- end %>
  </div>
</div>
